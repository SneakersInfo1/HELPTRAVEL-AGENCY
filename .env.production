# PODGLĄD (Vercel preview) — WŁĄCZA moduł „Pakiety Lot + Hotel" dla deploymentów z gałęzi preview/packages-*.
# Flaga publiczna (NEXT_PUBLIC_), inline w buildzie — musi być obecna PODCZAS BUILDU, żeby UI pakietów się pokazało.
# UWAGA: ta gałąź jest TYLKO do podglądu. NIE mergować do main jako-jest — produkcyjny rollout
# ustawia tę flagę świadomie w panelu Vercel (Settings → Environment Variables, scope Preview/Production).
NEXT_PUBLIC_FEATURE_PACKAGES=true
